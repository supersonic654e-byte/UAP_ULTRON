# Software (shared / cross-version)

Shared tools and libraries that span robot generations.

- Version-specific ROS workspaces live under `versions/<gen>/software/`.
- This folder is for reusable tooling: message packages, common launch
  fragments, analysis scripts, Docker base-image tooling.

Nothing here yet — planned: shared message/interface package and the VS Code
devcontainer spec.
