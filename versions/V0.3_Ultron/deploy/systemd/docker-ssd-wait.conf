# Docker must wait for the /mnt/ssd mount (Bible §3.4).
# Install: sudo cp docker-ssd-wait.conf /etc/systemd/system/docker.service.d/
#          sudo systemctl daemon-reload
[Unit]
RequiresMountsFor=/mnt/ssd
