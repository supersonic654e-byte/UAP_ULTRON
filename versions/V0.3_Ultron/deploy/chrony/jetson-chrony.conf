# Ultron_V0.3 Jetson chrony (Bible §4.5 B12) — NTP client of the laptop.
# Replace <LAPTOP_TS_IP> with the laptop Tailscale IP before installing.

# pool lines (default) are commented out; the robot syncs to the laptop only.
#pool 2.debian.pool.ntp.org iburst
server <LAPTOP_TS_IP> iburst

# Allow the laptop to query us if desired.
#allow 100.64.0.0/10
