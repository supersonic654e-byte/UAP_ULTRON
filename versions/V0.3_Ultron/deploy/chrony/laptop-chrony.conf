# Ultron_V0.3 Laptop chrony (Bible §4.5 B12) — NTP server for the robot.
# The laptop serves the 100.64.0.0/10 (Tailscale CGNAT) range.

# Keep upstream sync so the laptop itself stays accurate.
#pool 2.debian.pool.ntp.org iburst

# Serve the robot over Tailscale.
allow 100.64.0.0/10
