# V0.3 Pilot — Analysis Notebooks

Place analysis + plot notebooks here. They consume `feature_extract.py` output
(`features.csv` + `mission_summary.json`) and the anonymous feature artifacts
in `../missions/*/`.

Keep the "find patterns → predict" loop (AI_Architecture_SHORT.txt) offline on
the laptop; no hospital network needed for V0.3.
