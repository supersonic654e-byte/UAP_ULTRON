# Hardware (shared / cross-version)

Cross-version hardware assets that don't belong to one robot generation.

- Version-specific hardware (BOM, wiring, CAD) lives under `versions/<gen>/hardware/`.
- Shared reference material lives under `docs/90_reference/` (datasheets, shared BOM, CAD).

Add shared jigs, test fixtures, and reusable mechanical parts here.
